# ease-typewriter

Cycles through an array of phrases with a type/delete loop and a blinking cursor.

**Usage:** Include `style.css` and the inline script from `demo.html`. Modify the `phrases` array to change the text content. The cursor blinks via a CSS `@keyframes` animation.
